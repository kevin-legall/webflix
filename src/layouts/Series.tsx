import React from 'react';
import Navbar from "../components/Navbar";

const Series = () => {
    return (
        <main>
            <h1>Séries</h1>
        </main>
    );
};

export default Series;